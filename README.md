# property-value-ml-challenge
Machine learning challenge: predicting real estate property values from cadastral and transaction data. Ensemble of XGBoost, LightGBM and CatBoost models (category-specialized + pooled), with blend weights selected via 5-fold cross-validation to optimize prediction accuracy.
